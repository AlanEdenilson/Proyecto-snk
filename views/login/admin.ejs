<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <script src="https://kit.fontawesome.com/54b3975121.js" crossorigin="anonymous"></script>
    <title>Registro</title>
    <link rel="stylesheet" href="/stylesheet/Gerardo_estilos/admin.css">
</head>
<body>
    <img class="mm" src="/stylesheet/Gerardo_estilos/grrr.png" alt="">
    <div class="register-container">
        <h2>REGISTRO ADMINISTRADOR</h2>
        <form method="post" action="/admin">
            <div class="input-group">
                <input type="hidden" name="rol" value="1" >
            </div>
            <div class="input-group">
                <label for="fullname"><i class="fas fa-user"></i> Nombre </label>
                <input class="pass"  type="text" id="fullname" name="username" placeholder="Nombre Completo" value="<% if(typeof valores != 'undefined') { %><%= valores.username %><% } %>"> 
                <% if (typeof errors !== 'undefined') { %>
                    <% if ( errors.username ) { %>
                            <div>
                                <h2 class="error1" 
                                    
                                    style="font-family: 'Times New Roman', Times, serif;
                                    font: small-caps 200% serif;
                                    font-size: 18px;
                                    color: #9b599b;
                                    position: relative;
                                    top: -15x;
                                    height: 20%;
    
                                    "><i class='bx bxs-error-circle' ></i><%= errors.username %></h2>
                                </div>
                            
                      
                      <% } %>
                      <% } %>
            </div>
            <div class="input-group">
                <label for="email"><i class="fas fa-envelope"></i> Correo Electrónico</label>
                <input type="text" id="email" name="email" placeholder="Usuario" value="<% if(typeof valores != 'undefined') { %><%= valores.email %><% } %>">
                <% if (typeof errors !== 'undefined') { %>
                    <% if ( errors.email ) { %>
                            <div>
                                <h2 class="error2" 
                                    
                                    style="font-family: 'Times New Roman', Times, serif;
                                    font: small-caps 200% serif;
                                    font-size: 18px;
                                    color: #9b599b;
                                    
    
                                    "><i class='bx bxs-error-circle' ></i><%= errors.email %></h2>
                                </div>
                            
                      
                      <% } %>
                      <% } %>
            </div>
           
            <div class="input-group">
                <label for="password"><i class="fas fa-lock"></i> Contraseña</label>
                <input class="password" type="password" id="password" name="password" placeholder="Contraseña" value="<% if(typeof valores != 'undefined') { %><%= valores.password %><% }%>" >
                <i class="fa-solid fa-eye-slash" style="color: #919191;"></i>
                <% if (typeof errors !== 'undefined') { %>
                    <% if ( errors.password ) { %>
                            <div>
                                <h2 class="error3" 
                                    
                                    style="position: relative;
                                    font-family: 'Times New Roman', Times, serif;
                                    font: small-caps 200% serif;
                                    font-size: 18px;
                                    color: #9b599b;

    
                                    "><i class='bx bxs-error-circle' ></i><%= errors.password %></h2>
                                </div>
                            
                      
                      <% } %>
                      <% } %>
            </div>
            <div class="input-group">
                <label class="repit-password" for="confirm-password"><i class="fas fa-lock"></i> Repetir Contraseña</label>
                <input class="confirm-password" type="password" id="confirm-password" name="confirm_password"  placeholder="Repetir Contraseña" >
                <i class="fa-solid fa-eye-slash" style="color: #919191;" id="n1"></i>
                <% if (typeof errors !== 'undefined') { %>
                    <% if ( errors.confirm_password ) { %>
                            <div>
                                <h2 class="error4" 
                                    
                                    style="font-family: 'Times New Roman', Times, serif;
                                    font: small-caps 200% serif;
                                    font-size: 18px;
                                    color: #9b599b;
                                    
                                    
    
                                    "><<i class='bx bxs-error-circle' ></i><%= errors.confirm_password %></h2>
                             </div>
                            
                      
                      <% } %>
                      <% } %>
            </div>
            
            <a href="/" class="login-link">Ya tienes cuenta?</a>
            <button type="submit" class="register-button">Registrarse</button>
        </form>
    </div>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <script src="https://code.jquery.com/jquery-3.7.1.js"></script>
    <script>
        $(document).ready(function() {
            $('.fa-solid').click(function() {
                var input = $(this).siblings('input');
                var icon = $(this);
                
                if (input.attr('type') === 'password') {
                    input.attr('type', 'text');
                    icon.removeClass('fa-eye-slash').addClass('fa-eye');
                } else {
                    input.attr('type', 'password');
                    icon.removeClass('fa-eye').addClass('fa-eye-slash');
                }
            });
        });
    </script>



<script>
    $(document).ready(function() {
        // Datos enviados desde el servidor

        if (typeof correo !== 'undefined'){
            const data =  JSON.stringify(correo) 

// Almacenar los datos en localStorage
       localStorage.setItem('correo', data.correo);
       alert("almacenado extosamente")


        }
     

        // Actualizar el estado en la página

    });
</script>
   
</body>
</html>